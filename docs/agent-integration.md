# Agent Integration

Adapters are host-agnostic. A host may detect, install, inspect, and verify using its
own tooling, but emitted guidance stays a snippet that points back to deterministic
`specd context` and `specd verify` commands.
