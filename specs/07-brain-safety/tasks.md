# Tasks — 07-brain-safety

| id | role | files | depends-on | verify | acceptance |
|---|---|---|---|---|---|
| T1 | scout | internal/orchestration/ (session.go, driver.go, lease.go, acp.go, decide.go), reference/ recovery tests (read-only design input) | | `printf ok` | Map of session lifecycle + lease semantics; recovery-case checklist re-derived from v1 tests; confirms mission ids can be made deterministic (session/step/task) |
| T2 | craftsman | internal/orchestration/checkpoint.go, checkpoint_test.go | T1 | `go test ./internal/orchestration -run TestCheckpoint -race -count=1` | Checkpoint record {session, step, wave, decision, outstanding dispatches, lease}; AtomicWrite; write-ahead ordering (checkpoint durable before dispatch visible); serialization round-trip tests (R1) |
| T3 | craftsman | internal/orchestration/session.go, internal/orchestration/acp.go + tests | T1 | `go test ./internal/orchestration -run TestMissionID -race -count=1` | Deterministic mission ids (session/step/task); ACP ledger keyed by them; duplicate-id append refused (R3 groundwork) |
| T4 | craftsman | internal/orchestration/driver.go, internal/cmd/brain.go + tests | T2 | `go test ./internal/orchestration ./internal/cmd -run TestBrainCancel -race -count=1` | `brain cancel`: session marked cancelled (terminal), lease released, step/run refused exit 1, task/evidence state untouched; state machine running→cancelled\|complete only (R2) |
| T5 | craftsman | internal/orchestration/recover.go, recover_test.go, internal/cmd/brain.go | T2,T3 | `go test ./internal/orchestration -run TestBrainResume -race -count=1` | `brain resume`: reconstruct from last checkpoint + ledger delta; dispatch re-issued only when no ACP record with mission id; irreconcilable checkpoint/ledger conflict refuses exit 1 naming conflict (R3, R4) |
| T6 | craftsman | internal/orchestration/lease.go + tests, internal/cmd/brain.go | T4,T5 | `go test ./internal/orchestration -run TestLeaseExclusive -race -count=1` | Live lease blocks second start/resume on same spec; expired/orphaned lease recoverable by resume only; `brain status` shows running\|cancelled\|crashed\|complete + checkpoint step/time + lease holder/expiry (R5, R6) |
| T7 | craftsman | internal/orchestration/recover_test.go (crash-injection), scripts/stress-brain.sh | T5,T6 | `bash -c 'go test ./internal/orchestration -run TestCrashInjection -race -count=1 && ./scripts/stress-brain.sh'` | Kill points post-checkpoint/pre-dispatch and post-dispatch/pre-ledger converge on resume, zero double-dispatch; cross-process racing resumes yield exactly one holder |
| T8 | craftsman | docs/command-reference.md, docs/CHEATSHEET.md, docs/agent-integration.md, docs/decisions/ (pause/directive deferral ADR) | T4,T5 | `./scripts/docs-lint.sh` | cancel/resume/status semantics + recovery guarantees documented; pause/directive deferral recorded as ADR |
| T9 | validator | (read-only) | T7 | `go test ./... -race -count=2` | Full suite green twice |
