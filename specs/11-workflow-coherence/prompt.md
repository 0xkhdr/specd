# Workflow Coherence — stage prompt

Replace `<WAVE>` with `W0`, then `W1` … `W6`:

> Implement `<WAVE>` from `specs/11-workflow-coherence/tasks.md`. Read its README,
> requirements, design, and repo instructions. Work only this wave, follow task order, write tests
> first, preserve no-bypass/zero-dependency/reference-read-only invariants, run every verify plus
> required full checks, and mark rows only after green. Stop after this wave and report changes,
> tests, and blockers. Do not start next wave.
