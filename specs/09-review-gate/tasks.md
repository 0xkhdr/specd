# Tasks — 09-review-gate

| id | role | files | depends-on | verify | acceptance |
|---|---|---|---|---|---|
| T1 | scout | internal/core/embed_templates/, internal/core/scaffold.go, internal/core/gates/, internal/core/md.go | | `printf ok` | Confirms template embed + scaffold pattern to follow, section-parse machinery reusable for report extraction, approval-gate insertion point for completion transition |
| T2 | craftsman | internal/core/embed_templates/review_report.md, internal/core/scaffold.go + tests | T1 | `go test ./internal/core -run TestReviewScaffold -race -count=1` | go:embed template with slug, git HEAD, per-task sections (id/files/acceptance), verdict field, findings, reviewer identity; scaffold via AtomicWrite; existing same-HEAD report refuses overwrite without --force (R1, R2) |
| T3 | craftsman | internal/core/review.go, review_test.go | T1 | `go test ./internal/core -run TestReviewParse -race -count=1` | Field-extraction parser (human-edited tolerant, byte-stability not required — rationale in comment); missing/unknown verdict or missing HEAD ⇒ parse failure, never approve (R5) |
| T4 | craftsman | internal/core/commands.go, internal/cmd/review.go, internal/cmd/registry.go + tests | T2 | `go test ./internal/cmd -run TestReviewCmd -race -count=1` | `review` verb registered (phase metadata per spec 03: execution phase); scaffolds report; --force path tested (R1, R2) |
| T5 | craftsman | internal/core/gates/review.go, config loader, gate tests | T3 | `go test ./internal/core/gates -run TestReviewGate -race -count=1` | Opt-in `review.required` (default off); completion refused unless approve verdict at evidence-pinned HEAD; stale-HEAD approval refused; reject/needs-changes surfaces findings in gate output; pure function of on-disk state (R3, R4) |
| T6 | craftsman | docs/validation-gates.md, docs/command-reference.md, docs/CHEATSHEET.md, .claude/agents/pinky-auditor.md, docs/decisions/ (checklist deferral ADR) | T4,T5 | `./scripts/docs-lint.sh` | Gate semantics + HEAD-freshness documented; auditor-fills-report role guidance; self-review anti-pattern stated honestly; checklist-extraction deferral ADR recorded (R6) |
| T7 | validator | (read-only) | T5 | `go test ./... -race -count=1` | Full suite green; lifecycle e2e passes with review.required both off and on |
