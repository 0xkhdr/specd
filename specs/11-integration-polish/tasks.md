# Tasks — 11-integration-polish

| id | role | files | depends-on | verify | acceptance |
|---|---|---|---|---|---|
| T1 | scout | internal/core/scaffold.go, internal/core/embed_templates/, internal/cmd/init.go, internal/cmd/handshake.go, internal/mcp/ | | `printf ok` | Map of every asset init writes today; marker insertion points; handshake output shape; confirms help --json (spec 03) available for digesting |
| T2 | craftsman | internal/core/embed_templates/, internal/core/scaffold.go + tests | T1 | `go test ./internal/core -run TestManagedMarkers -race -count=1` | All managed regions wrapped in `<!-- specd:managed:<asset>:v<N> -->` markers; scaffold idempotent (twice = once, byte-identical) (R2) |
| T3 | craftsman | internal/core/scaffold.go, internal/cmd/init.go + tests | T2 | `go test ./internal/cmd -run TestInitRepair -race -count=1` | `--repair` restores drifted managed regions from template, content outside markers byte-untouched, repaired files reported; `--refresh` updates to current template version with same guarantee (R3, R4) |
| T4 | craftsman | internal/cmd/init.go + tests | T3 | `go test ./internal/cmd -run TestInitDryRun -race -count=1` | `--dry-run` prints unified-diff-style preview per file, writes nothing (fs snapshot identical before/after) (R5) |
| T5 | craftsman | internal/mcp/, internal/cmd/mcp.go, embedded snippet templates + golden tests | T1 | `go test ./internal/mcp ./internal/cmd -run TestMCPConfig -race -count=1` | `mcp --config claude-code` emits paste-ready snippet from embedded template (golden test, valid JSON); `--root`/`--spec` pinning honored; unknown host exits 2 listing known hosts (R1) |
| T6 | craftsman | internal/cmd/handshake.go + tests | T1 | `go test ./internal/cmd -run TestHandshakeDigest -race -count=1` | Handshake carries SHA-256 of canonical help --json payload + effective-config digest; `--expect-palette-digest`/`--expect-config-digest` mismatch exits 1 naming which drifted; digest stable across runs, changes when verb added (R6) |
| T7 | craftsman | docs/command-reference.md, docs/CHEATSHEET.md, docs/agent-integration.md, docs/user-guide.md, docs/decisions/ (multi-host deferral ADR) | T3,T4,T5,T6 | `./scripts/docs-lint.sh` | Repair-overwrites-inside-markers contract stated loudly; digest workflow for agents documented; multi-host detection deferral ADR |
| T8 | validator | (read-only) | T3,T4,T5,T6 | `go test ./... -race -count=1` | Full suite green |
