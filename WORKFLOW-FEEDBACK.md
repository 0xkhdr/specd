# Workflow feedback log

Append-only log of friction hit while *using* specd to build specs. Input for
improving the harness so agents work with specd as native knowledge.

Do not delete entries. Do not rewrite history — resolved entries get
`Status: resolved (<commit/spec>)`, they stay.

Two entry kinds: **friction** (something blocked or misled the agent) and
**improvement** (workflow succeeded, but a concrete change would make it
faster, clearer, or harder to get wrong).

## Friction format

```markdown
### <YYYY-MM-DD> — friction — <short title>
- **Context:** spec slug, phase, role, exact command run
- **Expected:** what the agent believed would happen and why
- **Actual:** exact output / exit code / blocker (shortest decisive line)
- **Root cause:** harness bug | missing guidance | ambiguous docs | agent error
- **Recommendation:** concrete change (verb/flag/gate/doc/message text)
- **Status:** open
```

## Improvement format

```markdown
### <YYYY-MM-DD> — improvement — <short title>
- **Context:** spec slug, phase, role, command sequence that worked
- **Observation:** what cost time or attention despite succeeding
- **Cost:** turns, re-reads, redundant commands, or context tokens burned
- **Recommendation:** concrete change (verb/flag/gate/doc/message text)
- **Tradeoff:** what it costs — invariant risk, added surface, or none
- **Status:** open
```

Improvement entries must clear a bar: an agent-hours or correctness win a
future run can feel. "Nice to have" is not an entry. Anything that would weaken
determinism, evidence integrity, or add a bypass gets logged with the tradeoff
stated plainly and stays a proposal — never a self-applied change.

---

### 2026-07-22 — friction — next program spec opens on an over-budget approved task
- **Context:** starting `workflow-03-state-foundations` at tasks phase, exact command `specd check workflow-03-state-foundations`
- **Expected:** the newly unlocked program frontier is ready for its human tasks→executing approval.
- **Actual:** `error context-budget: T16A: required context 40777 tokens exceeds budget 40000 — decompose the task or narrow declared files` followed by `GATE_FAILED: readiness plan 5083d7d5a8c97984b868ee731cfb62a3ade246c1c57dafb18e01749d8719bf3a has 1 blocker(s)`.
- **Root cause:** task decomposition gap — T16A combines lifecycle/gate integration, status projection, compatibility, and generated docs in one context-bound task.
- **Recommendation:** split T16A into an approval lifecycle/gate task and a dependent status/docs compatibility task, each with its own focused verify command.
- **Status:** open

### 2026-07-22 — friction — observer feedback creates an undeclared inventory repair task
- **Context:** final CI verification for `workflow-02-explicit-activation`, exact command `./scripts/test-lint.sh`
- **Expected:** required observer-mode entries can be appended during scoped task execution without creating a new out-of-scope code change.
- **Actual:** `test-lint: missing feedback inventory entry: WORKFLOW-FEEDBACK.md :: 2026-07-22 — friction — midreq records repair intent but leaves no executable recovery path` plus the four other new 2026-07-22 entries; the inventory file was outside every approved task.
- **Root cause:** task scope gap — AGENTS.md mandates immediate feedback writes, while structural lint also mandates synchronized `docs/workflow-regressions.md` rows, but neither file was in the implementation or repair task scopes.
- **Recommendation:** any spec task that drives the harness should automatically include the feedback log and regression inventory as conditional observer outputs, or lint should accept a dedicated final observer-sync task generated by the harness.
- **Status:** open

### 2026-07-22 — friction — midreq records repair intent but leaves no executable recovery path
- **Context:** `workflow-02-explicit-activation` after all approved tasks completed; exact command `specd midreq workflow-02-explicit-activation --scope final-verification --text "Add a repair task covering internal/core/agents_test.go, internal/cmd/e2e_test.go, internal/cmd/integration_polish_test.go, and internal/context/manifest_test.go; verify with go test ./... -race -count=1."`
- **Expected:** the governed amendment either creates/authorizes a repair task or guidance names the next legal authoring action.
- **Actual:** revision advanced to 12, but status remains `5 complete, 0 pending`, `tasks.md` remains T07–T11, agent authority is `none`, and `status --guide` only lists generic legal commands.
- **Root cause:** missing workflow — `midreq` appends audit/freshness records but has no governed task-amendment transaction, matching the documented deferred Release B+ gap.
- **Recommendation:** add a human-only `specd amend-task` transaction that appends a scoped task row, pins the amended plan, and exposes the new frontier without manual state edits.
- **Status:** open

### 2026-07-22 — friction — general-mode tool filtering also removes applicable skills
- **Context:** final verification for `workflow-02-explicit-activation`, exact command `go test ./... -race -count=1`
- **Expected:** explicit request-mode routing keeps context skill selection intact while filtering only command/tool exposure.
- **Actual:** `TestManifestFreshScaffoldSelectsApplicableSkills` reports `applicable skill not selected: skill:foundation@1.0.0; got map[]` and `applicable skill not selected: skill:execute@1.0.0; got map[]`; `TestManifestDriverLanes` reports `driver items = [{Kind:guardrails ...}]` with every tool and skill absent.
- **Root cause:** task scope gap — T10's general-mode permitted-operation set is reused as a capability filter by context selection, but `internal/context/manifest_test.go` was not declared and no task asserted that skills remain available independently of CLI commands.
- **Recommendation:** add `internal/context/manifest_test.go` to T10 and separate skill capability selection from request-mode command exposure; general mode should remove specd commands, not applicable local knowledge.
- **Status:** open

### 2026-07-22 — friction — request-mode routing invalidates undeclared context-route tests
- **Context:** `workflow-02-explicit-activation` T10, craftsman broader check, exact command `go test ./internal/core ./internal/cmd -count=1`
- **Expected:** the broader core/cmd suite remains green after the request-mode contract passes its declared verification.
- **Actual:** `--- FAIL: TestLifecycleE2EDefaultScaffold` with `e2e_test.go:354: generated context omitted "route": "cli::`; `--- FAIL: TestIntegrationMachineContextCarriesDriverContract` with `integration_polish_test.go:268: machine context missing "route": "cli::`; and `--- FAIL: TestIntegrationMachineContextRejectsRouteIdentityMismatch` with `integration_polish_test.go:290: context route identity mismatch: {`.
- **Root cause:** task scope gap — request-mode route filtering changed the default context contract, but the existing lifecycle/integration context-route tests are outside T10's declared files and no later task declares them.
- **Recommendation:** declare `internal/cmd/e2e_test.go` and `internal/cmd/integration_polish_test.go` in T10 (or the guide-conformance follow-up) and update their fixtures/assertions for explicit managed mode while retaining general-mode command freedom.
- **Status:** open

### 2026-07-22 — friction — canonical scaffold makes an existing Doctor fixture conflict
- **Context:** `workflow-02-explicit-activation` T09, craftsman broader check, exact command `go test ./internal/core ./internal/cmd -count=1 && go vet ./... && ./scripts/test-lint.sh && ./scripts/docs-lint.sh`
- **Expected:** the broader core/cmd suite remains green after T07–T09 complete their declared verification.
- **Actual:** `--- FAIL: TestDoctorChecksOrchestrationWorkerAlignment (0.05s)` and `agents_test.go:59: doctor missing actionable WORKER_DEFINITION_MISSING finding: [{Code:CONFIG_INVALID Severity:error Ref:/tmp/TestDoctorChecksOrchestrationWorkerAlignment280107566/001 Message:configuration sources conflict at keys agent, orchestration.enabled, verify.timeout_seconds, version: /tmp/TestDoctorChecksOrchestrationWorkerAlignment280107566/001/.specd/config.yaml, /tmp/TestDoctorChecksOrchestrationWorkerAlignment280107566/001/project.yml RecoveryAction:repair configuration sources, then run \`specd agents doctor --json\` again}]`.
- **Root cause:** task scope gap — T08 changed fresh scaffold output to canonical `.specd/config.yaml`, but the existing Doctor fixture still adds conflicting legacy `project.yml`; its `internal/core/agents_test.go` file was not declared by T08.
- **Recommendation:** add `internal/core/agents_test.go` to the canonical-scaffold task scope and update the fixture to mutate the canonical config instead of creating a conflicting legacy source.
- **Status:** open

### 2026-07-22 — friction — `status --help` is parsed as a spec slug
- **Context:** locating the active program spec, exact command `go run . status --help`
- **Expected:** status-specific help or the registered status usage with exit 0.
- **Actual:** `usage: specd status [spec] [--json] | specd status <spec> --guide [--json] | specd status --program` followed by `exit status 2`.
- **Root cause:** missing guidance — the handler treats `--help` as invalid input instead of routing to command help.
- **Recommendation:** route `<verb> --help` through the existing `specd help <verb>` palette path before handler argument validation.
- **Status:** open

### 2026-07-20 — friction — `specd help link` omits the allowed `--kind` values
- **Context:** authoring spec `agent-driver-protocol`, phase perceive, role craftsman. `specd link agent-driver-protocol agent-protocol-clarity --kind depends-on --reason "..."`
- **Expected:** `depends-on` accepted, or the allowed set visible in `specd help link` before the call. Help printed `--kind  Link kind (default: follows).` with no enumeration.
- **Actual:** `usage: flag --kind="depends-on" not allowed; expected one of [follows regresses maintains supersedes]` (exit 2)
- **Root cause:** missing guidance — the enumeration exists in the validator but not in the palette help text.
- **Recommendation:** render enumerated flag values in `specd help <verb>`, e.g. `--kind  Link kind: follows|regresses|maintains|supersedes (default: follows).` Palette is the single source of truth, so this is one field, and `docs-lint.sh` keeps the reference in sync.
- **Status:** open

### 2026-07-20 — friction — steering templates never load into the machine manifest
- **Context:** fresh `git init` + `specd init` + `specd new demo` in a throwaway tree, specd 1.0.0. `specd context demo T1 --json`
- **Expected:** the five shipped steering templates appear in `items`, since `specd context demo T1` lists all six.
- **Actual:** all five in `omissions` with reason `missing explicit applicability metadata`; `grep -rl specd-context internal/core/embed_templates/steering/` returns nothing.
- **Root cause:** harness bug — no shipped steering template carries the `specd-context` block `SelectSteering` requires, so every driver runs with no project constitution and nothing reports an error.
- **Recommendation:** ship a permissive `specd-context` block in each steering template, and make total steering omission a warning-severity `specd check` finding. Tracked as spec `template-conformance` R1.
- **Status:** open

### 2026-07-20 — friction — requirements scaffold cannot pass `ParseRequirements`, and the error blames tasks.md
- **Context:** same throwaway tree; scaffolded `requirements.md` emits `## Requirement R1 — <name>` and `- **R1.1** When …`
- **Expected:** filling the scaffold as written yields parseable requirement IDs.
- **Actual:** `RequirementIDSet` is empty because the parser wants `## R1 —` and `- R1.1:`; the failure surfaces as `error task-trace: T1 references unknown requirement R1.1` — against the wrong file, for a reference that is correct.
- **Root cause:** harness bug — template/consumer contract with no test asserting it, plus displaced error attribution.
- **Recommendation:** correct the template to the parsed format, and when `RequirementIDSet` is empty say so against `requirements.md` instead of reporting an unknown reference against `tasks.md`. Tracked as spec `template-conformance` R2.1/R3.1.
- **Status:** open

### 2026-07-20 — friction — tasks template teaches `evidence=tests`, which the quality-declaration gate rejects
- **Context:** same throwaway tree; `tasks.md` example comment ships `evidence=tests`.
- **Expected:** the template's own example value is accepted.
- **Actual:** `QUALITY_DECLARATION_INVALID: "tests" must be class/check-id (valid classes: test, output_eval, trajectory_eval, review)`
- **Root cause:** harness bug — same untested template/consumer contract.
- **Recommendation:** change the example to `evidence=test/<check-id>`. Tracked as spec `template-conformance` R2.2.
- **Status:** open

### 2026-07-20 — friction — task `files:` cannot express the test file its own `verify` line requires
- **Context:** executing spec `agent-protocol-clarity` T1, role craftsman. Task declares `files: internal/core/roles.go` and `verify: go test ./internal/core -run TestRoleCapabilityContract -count=1`.
- **Expected:** the declared file set covers everything the task must touch to make its own verify line pass.
- **Actual:** `TestRoleCapabilityContract` did not exist, so the verify line could only pass by editing `internal/core/roles_test.go` — a file the task never declared. `.specd/roles/craftsman.md` says "Touch only files explicitly named in the task's `files:`; tests must also be declared", so the role text names the obligation the task row cannot satisfy. Nothing blocked: `specd verify` and `specd complete-task` both exited 0 against an undeclared write.
- **Root cause:** authoring gap with no gate behind it — a `verify` line naming `-run <TestName>` implies a test file, and no check asserts that file is declared.
- **Recommendation:** in the tasks gate, when a `verify` line contains `-run <TestName>`, require at least one `_test.go` path in that task's `files:`. Fails closed at authoring time, costs one regexp, and needs no new state. Deterministic — it reads the tasks table only.
- **Status:** open

### 2026-07-20 — friction — slug traversal is guarded at ~29 call sites, never at the path builders
- **Context:** executing `agent-protocol-clarity` T5. A new typed-refusal test drove `specd check ../../escape` and found a second, independent traversal-rejection path in `loadSpec` (`internal/cmd/registry.go`) that the migrated `checkPhase` route never reaches — `check` declares `PhaseAny`, so the phase gate returns before validating.
- **Expected:** one chokepoint rejects a traversal slug, or the sink refuses to build the path.
- **Actual:** `grep -rn ValidateSlug internal/ --include="*.go"` returns ~29 non-test call sites. Every spec path builder (`StatePath`, `EvidencePath`, `SpecMemoryPath`, `EvalStorePath`, `RunLedgerPath`, ~20 total) takes a slug and `filepath.Join`s it with **no** validation, e.g. `filepath.Join(SpecdDir(root), "specs", slug, "memory.md")`. Safety depends entirely on every caller remembering. Probed all 29 spec-taking verbs against a live tree: **no escape today** — this is fragility, not a live vulnerability.
- **Root cause:** defense placed at callers instead of the sink, with no palette field marking "this verb takes a slug" (`SpecSlugArg` is set only on phase-enforced verbs; usage strings spell it `<spec>`, `<slug>`, `<from-slug>`, `<new-spec>` inconsistently). The existing `TestSlugTraversalRejected` was a hand-maintained list of 15 verbs, so `drift`, `archive`, `spike`, `brain`, `unlink` and any future verb were uncovered.
- **Recommendation:** add `core.SpecDir(root, slug string) string` as the single join for everything under `.specd/specs/<slug>/`, and route all ~20 builders through it. `url.PathEscape` on the slug inside it is a no-op for valid slugs (`^[a-z0-9][a-z0-9-]*$`) and neutralizes traversal, so it is defense-in-depth with zero behavior change and no signature churn. Separately, add a `TakesSpecSlug` bool to the palette so coverage tests stop inferring it from usage prose. Needs its own task — outside T5's declared files.
- **Status:** open


### 2026-07-20 — friction — `evidence` cell can declare a `review/` check no role in the task can produce
- **Context:** executing `agent-protocol-clarity` T8, role auditor. Task declares `evidence: review/protocol-clarity-audit` and `verify: go test ./... -race -count=1`.
- **Expected:** running the task's own `verify` line and then `complete-task` closes the task.
- **Actual:** `specd verify` recorded evidence, then `complete-task` refused: `EVIDENCE_MISSING: task T8 lacks passing evidence for review/protocol-clarity-audit; review/protocol-clarity-audit: a plain `specd verify` record carries no evidence class and cannot satisfy it — import external evidence with `specd eval import <slug> <file> --task T8 --check protocol-clarity-audit``
- **Root cause:** authoring gap with no gate behind it. A task can pair a `verify` line with an `evidence` class that `verify` structurally cannot produce, and nothing says so until `complete-task` — after the work is done. The refusal message is good (it names the exact recovery command); the cost is that it arrives at the end rather than at `specd approve` on tasks.
- **Recommendation:** in the tasks gate, when an `evidence` cell declares a non-`test/` class, require the task to be authored with that producer in mind — at minimum warn at approve time that its `verify` line cannot satisfy the declaration. Deterministic: reads the tasks table only.
- **Status:** open

### 2026-07-20 — friction — role/steering conformance has no equivalent of the roles capability check for steering text
- **Context:** auditing `agent-protocol-clarity` T8 against R1.2 ("any shipped role **or steering** text ... shall fail a test if that command is denied to the role it is written for").
- **Expected:** the spec's own conformance test covers both surfaces R1.2 names.
- **Actual:** `internal/core/embed_templates/steering/workflow.md:25` still reads "Record deviations from the spec via `specd decision` before finishing a task." `specd decision` is `HumanOnly: true`, so this instructs every agent to run a command its authority denies. `TestRoleProseMatchesCapability` (added by T2) scans `internal/core/embed_templates/roles/` only, and no task in this spec declares the steering directory in its `files:`, so the violation survives a fully green spec.
- **Root cause:** the requirement names two surfaces; the task decomposition covered one. Steering text is not role-scoped, so the "denied to the role it is written for" check needs a target role (craftsman) to be meaningful.
- **Recommendation:** add a craftsman-scoped conformance case over `internal/core/embed_templates/steering/*.md` and change that line to `specd request-decision` (added by T3). Needs its own task — outside every declared `files:` in this spec.
- **Status:** open

### 2026-07-20 — improvement — every task complete and `check` clean, but criterion coverage reads 0/12 with no next action named
- **Context:** `agent-protocol-clarity` after T1–T8 all completed. `specd check agent-protocol-clarity` exits 0 with no findings; `specd status agent-protocol-clarity` ends with `total 0/12 criteria passing`.
- **Expected:** either the coverage number moves as tasks complete, or the status output names the command that moves it.
- **Actual:** eight tasks with `acceptance` cells naming R1.1 … R6.2 all completed against passing evidence, and coverage stayed 0/12. `specd status --guide` lists 29 legal commands and never mentions criterion verification, so the only route to a non-zero number is knowing `verify_criterion` exists from the MCP palette.
- **Root cause:** task-level evidence and criterion-level coverage are separate ledgers with no link, and the guidance surface that exists to say "what next" does not mention the second one. The number is correct; it is just unreachable from the guidance.
- **Recommendation:** when a spec has completed tasks whose `acceptance` cells reference criteria with no criterion evidence, add a `specd verify-criterion <slug> <criterion>` line to `status --guide` blockers. Deterministic — reads the tasks table, state, and the criterion store.
- **Status:** open

### 2026-07-20 — improvement — bare `specd verify` usage line hides `--criterion` mode, which `help verify` and the docs both document
- **Context:** `agent-protocol-clarity` after T1–T8 complete, chasing the `total 0/12 criteria passing` line from the entry above. Went looking for the command that records criterion evidence.
- **Expected:** the first surface I hit while holding a spec with uncovered criteria names the command that covers them.
- **Actual:** guessed a top-level verb and got `UNKNOWN_COMMAND: unknown command "verify-criterion"`; the palette dump that follows it lists `verify` with no hint the mode exists. Ran `specd verify` bare and got `usage: specd verify <slug> <task>` — the task form only. Only `specd help verify` and `docs/command-reference.md:821` show the real two-mode line: `specd verify <slug> --criterion <r>.<n> --status pass|fail --evidence <text>`. No parity gap and no missing verb — the feature is complete and correctly documented, it just is not visible from the two surfaces reached first.
- **Root cause:** `runVerify` (`internal/cmd/verify.go:22`) hand-writes a shorter usage string than the palette's own `help verify` text, so the dispatch-time error is narrower than the registered usage. The two strings are independent, with nothing asserting they agree.
- **Recommendation:** return the palette usage string in `runVerify`'s arity error instead of the hand-written literal, so the error matches `help verify`. One line, removes a class of drift rather than this instance. Optionally have `docs-lint.sh` assert no `errors.New("usage: ...")` literal in a handler whose verb has a palette usage string.
- **Status:** open

### 2026-07-20 — friction — `specd context` refuses any task that declares a directory, blaming the wrong cause
- **Context:** starting `agent-driver-protocol` T1 (role scout), whose `files` cell is `internal/cmd,internal/core,internal/orchestration,internal/mcp` — four directories, because T1 is a read-only survey of existing primitives rather than an edit to named files.
- **Expected:** `specd context agent-driver-protocol T1 --json` returns the bounded manifest, per the host guide's step 3.
- **Actual:** `context source "internal/cmd": missing or unreadable` — exit 1, no manifest. `internal/cmd` exists, is a readable directory, and is listed by `ls -d`. Reproduced identically on the stale installed binary (`5babefe`) and a fresh build of HEAD (`99208ce`), so it is not version skew.
- **Root cause:** `internal/context/selector.go:44` calls `os.ReadFile` on every declared file after `ResolveSource` has already approved it. `ResolveSource` (`internal/context/resolver.go:27`) uses `filepath.EvalSymlinks`, which succeeds on directories, so a directory passes resolution and then fails the read with `EISDIR`. The `EISDIR` error is discarded and rewritten as the fixed string `"missing or unreadable"`, which is false on both counts and sends you looking for a path typo or a permissions problem that does not exist.
- **Impact:** any task declaring a directory cannot obtain a context manifest at all. In this spec that is T1 (scout survey) and T10 (auditor, `internal/core,internal/cmd,internal/orchestration,internal/integration,docs`) — the first and last tasks, both read-only roles, which are exactly the roles whose scope is naturally directory-shaped. The protocol step is unavailable and the only way forward is to skip it.
- **Recommendation:** decide whether directories are legal in a `files` cell, then enforce that decision at one end. Either (a) expand a declared directory to its files during selection so scout and auditor tasks get a real manifest — note this needs a bound, since `internal/core` alone would blow the context budget, or (b) reject directories in the tasks gate at `specd approve`, so the failure lands at authoring time with a message naming the offending cell, rather than mid-execution. Independently and regardless of which is chosen: stop collapsing the `os.ReadFile` error to a fixed string — surface `EISDIR` as "is a directory, not a file", since the current text misdescribes a condition the caller can otherwise see is untrue.
- **Status:** open

### 2026-07-20 — friction — `specd context` is unavailable to every task that creates a file
- **Context:** starting `agent-driver-protocol` T2 (role craftsman, `kind: feature`), whose `files` cell is `internal/core/session.go,internal/cmd/session.go,internal/core/commands.go`. The first two do not exist — creating them *is* the task.
- **Expected:** `specd context agent-driver-protocol T2 --json` returns the bounded manifest, per the host guide's step 3.
- **Actual:** `required source: context source "internal/cmd/session.go": missing or unreadable` — exit 1, no manifest.
- **Root cause:** same line as the directory entry above (`internal/context/selector.go:44` reads every declared file eagerly and marks it `Required: true`), but a distinct condition with a distinct fix. There, an existing directory failed with `EISDIR`; here a not-yet-created file fails with `ENOENT`. The selector treats "declared" as "must already exist on disk", which is true for a task that edits and false for a task that creates.
- **Impact:** broader than the directory case. Every `kind: feature` task that introduces a new file cannot obtain a context manifest, which is most of this spec — T2, T3, T4, T5, T6, T8 all declare at least one file they are meant to create. Combined with the directory entry, 8 of this spec's 10 tasks cannot complete step 3 of the documented task loop. The loop's own guide presents `context` as mandatory before mutable work, so the documented happy path is unreachable for the common case of writing new code.
- **Recommendation:** distinguish "declared file that must be read" from "declared file that may be written". A declared path that does not exist is not an error for a task whose role has write authority — it should appear in the manifest as a write target with no content, no digest, and `Required: false`, so the path stays in scope for authority while contributing nothing to the context budget. Only a *missing read source* (requirements, design, role) should refuse. As with the directory entry, stop collapsing the `os.ReadFile` error to a fixed string: `ENOENT` and `EISDIR` are different conditions and neither is "unreadable".
- **Status:** open

### 2026-07-20 — friction — the trivial-verify gate catches `printf ok` but not a `-run` pattern that matches no tests
- **Context:** executing `agent-driver-protocol` T2 and T3. A fixture task authored as `| T1 | craftsman | drive-proof.txt | - | printf ok | R1 |` was correctly refused by the gate: ``T1 is a write task with trivial verify "printf ok"; a write task must verify its change``. That gate works.
- **Expected:** the same reasoning applies to any verify line that cannot fail because of the change under test.
- **Actual:** T2's declared verify is `go test ./internal/core ./internal/cmd -run TestDriverSession -count=1`. The `internal/cmd` half printed `ok github.com/0xkhdr/specd/internal/cmd 0.007s [no tests to run]` and the command exited 0, so it recorded passing evidence for a package in which nothing ran. T3's line has the same shape. In this spec T4, T5, T6, T7 and T8 all declare `go test ./<pkg> -run Test<Name>`, so any of them whose test is misnamed, or placed in a package the pattern does not reach, completes against a green run of zero tests.
- **Root cause:** the trivial-verify gate pattern-matches known-inert commands (`printf`, `true`, `:`) rather than reasoning about whether the command is coupled to the declared files. `go test -run <pattern>` is a real command that exits 0 when the pattern matches nothing — inert in effect, not in form, so the existing check cannot see it. Evidence integrity is not violated: the record is real and pinned to a resolvable HEAD. The gap is that the record can attest to nothing having run.
- **Impact:** a task can complete with a passing verify record and zero executed assertions. This is the failure mode most likely to look green while shipping nothing, and it is invisible at completion time — I only noticed it by reading the captured `go test` output, which `complete-task` does not surface.
- **Recommendation:** `go test` already reports this — `[no tests to run]` appears in stdout, and `-run` with no match is distinguishable from a real pass. Have the verify recorder scan captured output for `no tests to run` and refuse to record the run as passing evidence, naming the pattern that matched nothing. Deterministic, reads only the captured output, and needs no new gate. Considered and rejected as too broad: requiring every verify line to name a declared file — legitimate lines like `./scripts/regress-domains.sh` (T9) name none.
- **Status:** open

### 2026-07-20 — friction — T4 cannot satisfy its own acceptance criterion within its declared files
- **Context:** executing `agent-driver-protocol` T4, declared `files: internal/core/gates/diffscope.go,internal/core/gates/registry.go`, acceptance `R4.1, R4.2, R4.3, R4.4, R4.5`.
- **Expected:** a task's declared files are sufficient to meet the acceptance criteria it names.
- **Actual:** R4.5 reads "When the diff-scope comparison runs, the system shall run it as a core invariant on every transport rather than only under the production profile." The profile branch that R4.5 removes lives at `internal/cmd/lifecycle.go:258` (`if cfg.ProductionTaskAuthorityRequired() {`), which T4 does not declare. Meeting R4.5 also required a git baseline on the driver session (`internal/core/session.go`, declared by T2) so non-production work has something to measure against. Three of the four files the change needed were undeclared. `internal/core/gates/registry.go` *was* declared and turned out not to need editing at all: `CheckCtx` gates are documented pure ("the caller reads files and state, the gates never touch disk", `internal/core/gates/core.go:19`) and diff-scope must shell out to git, so it is a function rather than a registry entry.
- **Root cause:** the `files` cell was authored from where the new code would live, not from where the behaviour change lands. A requirement phrased as "stop doing X only under condition C" always touches the site that tests C, which is rarely the same file as the new logic. Nothing checks that a task's declared files can plausibly satisfy its declared acceptance criteria, so the mismatch surfaces only when an agent is mid-task and has to choose between violating scope and missing acceptance.
- **Impact:** this is the first task in this spec where the declared scope and the acceptance criteria were in direct conflict, and the conflict was invisible until implementation. Resolved by asking the human, who chose to edit both and log the deviation. Note the diff-scope gate this task adds would itself have refused this task's own diff — the spec is now strict enough to reject the work that builds it, which is the correct outcome and worth stating plainly.
- **Recommendation:** at `specd approve` on the tasks gate, warn when a task's acceptance references a requirement whose text names a behaviour ("rather than", "instead of", "on every") while the task declares only new files — too fuzzy to gate on. The deterministic version: once diff-scope is enforced everywhere, a task that refuses its own completion because of an undeclared file it had to touch is itself the signal. Surface that as a distinct refusal ("task T4 touched N files outside its declaration; amend the files cell and re-approve") rather than the generic OUTSIDE_SCOPE, so the recovery is "fix the declaration", not "revert the work".
- **Status:** open

### 2026-07-20 — friction — a requirement satisfied by an existing function reads as unbuilt, and the real gap sits one file outside the task
- **Context:** executing `agent-driver-protocol` T7, declared `files: internal/orchestration/lease.go,internal/orchestration/decide.go`, acceptance `R6.1, R6.2, R6.3`.
- **Expected:** three requirements, three pieces of work, in the two declared files.
- **Actual:** the three split differently than the task assumed. R6.2 ("detect the overlap before dispatch") was already implemented — `CheckParallelConflict` (`internal/orchestration/conflict.go:15`), called from `internal/cmd/brain_claim.go:39` — but at *claim* time, not dispatch, so the conflicting mission is minted and then refused. R6.1 had no implementation at all: `Lease` and `core.DriverSession` were two independent authorities over the same task with no field linking them. R6.3 was the interesting one — `ValidateWorkerReport` (`internal/orchestration/report.go:24`) checks `r.SubjectHead == ""` but never compares it to `m.SubjectHead`, so a worker reporting against a drifted baseline passes the identity check by carrying *any* non-empty head. That fix belongs in `report.go`, which T7 does not declare.
- **Root cause:** two distinct authoring gaps. First, the tasks table has no way to say "this requirement is already met by X, verify rather than build" — so a task carrying a satisfied requirement looks identical to one carrying an unbuilt one, and the only way to tell is to read the code. Second, and the same shape as the T4 entry above: the requirement's natural home (`report.go`, `conflict.go`) was not the file the task declared, because the `files` cell was authored from where the *new* code would go.
- **Impact:** R6.3 is a real hole that a fully green spec would have shipped. Nothing in the suite compared report and mission subject heads, and the requirement naming it was assigned to a task whose declared files could not reach the code. The near-miss is that "detect overlap" and "reject stale report" both *sounded* covered by existing orchestration machinery, and only reading `ValidateWorkerReport` line by line showed one of them was not.
- **Recommendation:** two things, both cheap. Add an explicit `kind: verify` alongside `feature`/`test`/`review` in the task schema, meaning "assert an existing invariant, expect no production diff" — a task claiming `feature` that produces no non-test diff is then a deterministic warning rather than a silent pass. Separately, when a task's acceptance cites a requirement, have `specd check` report which files in the repo already mention that requirement id in a comment or test name; a requirement whose only mentions are outside the task's declared files is the exact signal that surfaced here, and it is a grep, not a judgement call.
- **Status:** open

### 2026-07-20 — friction — `specd eval import` rejects an absolute artifact path with a bare usage error
- **Context:** executing `template-conformance` T8 (auditor). T8's evidence cell declares `review/template-conformance-audit`, a review-class contract `specd verify` cannot satisfy; the completion refusal correctly pointed to `specd eval import template-conformance <file> --task T8 --check template-conformance-audit`.
- **Expected:** pass the built evidence envelope from anywhere and have it imported.
- **Actual:** `specd eval import template-conformance /tmp/.../t8-eval.json --task T8 --check ...` failed with `usage: absolute artifact path not allowed`. No mention of which argument was absolute, why the restriction exists, or that a repo-relative path is required. Copying the file under `.specd/specs/template-conformance/` and passing the relative path worked first try.
- **Root cause:** the path guard rejects absolute paths (reasonable — keeps artifacts inside the workspace) but reports it as a generic `usage:` line rather than a refusal that names the offending path and the fix.
- **Impact:** the refusal that sends you to `eval import` gives a full ready-to-run invocation, so the natural next step is to point it at the file you just built — which for a scratch-built envelope is an absolute temp path. The tool then rejects exactly the input its own guidance implies, with a message that does not say "use a workspace-relative path".
- **Recommendation:** make the guard a typed refusal: `ARTIFACT_PATH_ABSOLUTE: artifact path %q must be relative to the workspace root; copy it under .specd/ and pass the relative path`. Deterministic, names the path, states the fix. Optionally have the completion refusal that prints the `eval import` invocation use `<workspace-relative file>` as the placeholder rather than `<file>`.
- **Status:** open

### 2026-07-20 — friction — per-task verify names a test in a package whose declared files cannot contain it
- **Context:** executing `template-conformance`. Several tasks declare a verify like `go test ./internal/context -run TestScaffoldedSteeringSelects` (T1) or `go test ./internal/core/gates -run TestSteeringTotalOmission` (T2), but the task's `files` cell lists only production files (steering templates, `steering.go`, `registry.go`) — never the `_test.go` file the verify names. No task's declared files include the test file its own verify depends on; T7 declares one test file (`internal/core/template_conformance_test.go`) but the steering conformance case must live in `internal/context` because `context` imports `core`, so a single core test file cannot reach `SelectSteering`.
- **Expected:** a task's declared files are sufficient to produce the passing verify the same task requires.
- **Actual:** to make each verify real I had to create the named test file in the package the verify targets — a file outside the task's declaration. Because no driver session was open, diff-scope was unenforced (`enforceDiffScope` returns nil with no baseline on the default profile), so completion succeeded; under a pinned baseline every one of these tasks would have refused its own completion for touching an undeclared test file. Left literal, the verify lines pass vacuously — `go test -run <Name>` exits 0 with `[no tests to run]` when the named test does not exist — so the tasks would "pass" having asserted nothing (compounding the earlier no-tests-to-run entry).
- **Root cause:** the `files` cell is authored as the production surface only; the test that proves the task is treated as implied rather than declared. Combined with `go test -run` exiting 0 on no match, a task can be authored, verified, and completed without the asserting test ever existing.
- **Recommendation:** require a task whose verify is `go test ... -run <Name>` to declare the `_test.go` file that defines `<Name>`, and have `specd check` warn when a `-run` pattern names a test not found in any declared file. Deterministic (a grep over declared files for `func <Name>`), and it closes the same gap the no-tests-to-run entry names from the authoring side rather than the recording side.
- **Status:** open

### 2026-07-21 — friction — `specd init --repair` cannot repair the layout finding it is told to repair
- **Context:** managed project `aido` (fresh `specd init`, no specs yet), orchestration armed in `project.yml`. `specd agents doctor --json` then `specd init --repair`.
- **Expected:** the finding's own `recovery_action` fixes it.
- **Actual:** doctor: `"code": "LAYOUT_MISSING", "ref": ".specd/specs", "message": "required agent-driving layout is missing", "recovery_action": "run \`specd init --repair\`"`. `specd init --repair` prints `all managed regions already in sync` and the finding persists — `internal/core/scaffold.go` never creates `.specd/specs`, only `specd new <slug>` does.
- **Root cause:** harness bug — doctor requires a directory the scaffold does not create, and points at a verb that cannot create it.
- **Recommendation:** either have `scaffold` mkdir `.specd/specs` (empty, tracked with `.gitkeep`), or downgrade the finding to `severity: warn` with `recovery_action: "run \`specd new <slug>\` to create the first spec"`. A doctor that is unhealthy on every correctly-initialised project trains agents to ignore it.
- **Status:** open

### 2026-07-21 — friction — `project.yml` list separators disagree, and inline `#` comments are parsed as value
- **Context:** authoring `project.yml` for `aido` from the shipped template. `specd agents doctor --json`.
- **Expected:** `routing.recommendations: low=standard;medium=standard;high=reasoning` parses like the `;`-separated `routing.class_capabilities` one line above it, and `max_verify_fails: 3    # consecutive verify failures...` parses as `3` like YAML.
- **Actual:** `"message": "routing.recommendations must use complexity=class entries"` and `"message": "escalation.max_verify_fails must be integer >= 0"`. `parseRoutingRecommendations` splits on `,` (config_validate.go:13) while `parseClassCapabilities` splits on `;` (config_validate.go:302); the loader only skips whole-line comments (config_loader.go:303).
- **Root cause:** harness bug (inconsistent separators) + missing guidance (the template documents neither the separator per key nor the whole-line-comment-only rule).
- **Recommendation:** accept both `,` and `;` in every config list, and strip an unquoted trailing `# ...` in the loader. Minimum: state "inline `#` comments are not supported; comments must be whole lines" at the top of the shipped `project.yml`, and show one worked `recommendations:` example next to `class_capabilities:` so the separator difference is visible.
- **Status:** open

### 2026-07-21 — friction — `specd new --agent=<name>` is a documented no-op
- **Context:** managed project `aido`, pre-spec, choosing how to start the first spec under armed orchestration. `specd help new` / `docs/command-reference.md` §`new`.
- **Expected:** `--agent` binds the spec to a worker harness, per the palette help (`--agent  Select agent harness.`) and the three shipped examples, including `specd new payments --agent=pinky`.
- **Actual:** `runNew` (internal/cmd/lifecycle.go:24) never reads its `flags` argument — it writes the four stubs and `InitialState(slug)` and returns. Nothing in `internal/cli` consumes it either. The flag parses, exits 0, and changes nothing on disk. The harness that actually matters is the root `agent:` key in `project.yml`, which `WorkerDefinitions` resolves and which rejects `pinky` outright (`handshake agent pinky is not a supported worker harness (want codex or claude)`).
- **Root cause:** harness bug — palette declares a flag with no handler, and its example advertises a value the rest of the system rejects.
- **Recommendation:** drop `--agent` from the `new` palette entry (subtractive bias — `project.yml agent:` is already the single source of truth), or wire it to record the harness on the spec's state and validate it against `pinkyFilesForHarness`. Either way delete the `--agent=pinky` example. A test asserting every declared flag is read by its handler would catch the whole class.
- **Status:** open

### 2026-07-21 — improvement — no unattended story, and `human_only` is unenforced metadata
- **Context:** managed project `aido`, pre-spec. Operator asked how to configure specd to skip approvals for an automated end-to-end test run. Searched the config surface: `grep -rni "auto_approve\|skip_approval\|unattended" internal/ docs/` returns nothing; no `SPECD_*` env var covers it either.
- **Observation:** the correct answer is "there is no knob, approval is a verb" — but nothing on the CLI or in the docs says so, and `HumanOnly` (internal/core/commands.go:106) is metadata that `runApprove` never checks. Any agent that reads `docs/command-reference.md` learns `approve` is "Human only", tries it anyway, and it works. So the invariant holds by convention while being trivially and *silently* violable, and the state history cannot distinguish a human approval from an agent's.
- **Cost:** one config search that could only end in a negative result, and a real risk that unattended runs quietly produce specs whose approval records mean nothing.
- **Correction (same day):** the actor *is* already recorded — `Record.Actor` (internal/core/state.go:62), stamped by `recordActor()` from `$SPECD_ACTOR`, else the OS username, else `unknown`. The gap is narrower than first written: the field is a free string with no human/agent distinction, and an agent shelling out as the logged-in user records that username, so a delegated approval is byte-identical to a hand-typed one.
- **Recommendation:** make delegation declarable rather than inferable — an opt-in `approval.delegation` config key plus a `delegated` flag on the approval record, and enforce `HumanOnly` at runtime against it. Full analysis and options in `unattended-approval-analysis.md` (repo root).
- **Tradeoff:** config key plus one record field; no gate weakens — `approve` still refuses on failing readiness gates either way. Strictly adds signal.
- **Status:** open (analysis written 2026-07-21)

### 2026-07-21 — friction — two gates read the same `evidence` cell with different parsers, and no cell satisfies both
- **Context:** managed project `aido`, spec `aido-config`, `project.yml` `profile: production`. `specd approve aido-config` (tasks → executing).
- **Expected:** a task declaring `evidence: test/integration-cli-config-show` with `kind: integration` and negative checks satisfies the boundary evidence policy.
- **Actual:** `error evidence-policy: external boundary lacks integration evidence planning`. Two authoring attempts (a `kind=integration` task in tasks.md, then an explicit integration-evidence plan in design.md) refused with byte-identical text. Reproduced directly against `BoundaryEvidenceFindings` + `ParseQualityContract` for one row (`Checks: "error path negative"`, production, design declaring an external boundary):
  ```
  cell="test/integration-cli-config-show"   boundary=1 finding   quality_err=nil
  cell="output_eval/integration-cli"        boundary=1 finding   quality_err=nil
  cell="test/integration-cli, integration"  boundary=0 findings  quality_err=QUALITY_DECLARATION_INVALID
  cell="integration"                        boundary=0 findings  quality_err=QUALITY_DECLARATION_INVALID
  cell="test/integration-cli;integration"   boundary=0 findings  quality_err=nil
  ```
- **Root cause:** harness bug — two gates armed at the same transition parse the same cell under contradictory rules. `BoundaryEvidenceFindings` (internal/core/evidence_policy.go:13) splits on `,`/`;` and requires a token `strings.EqualFold(..., "integration")` — bare, no slash. `ParseQualityContract` (internal/core/quality_contract.go:32), armed by `qualityDeclaration` for `ApproveTarget == executing`, splits on `,`/space and rejects any token without `/` and any class outside `test|output_eval|trajectory_eval|review`. So bare `integration` fails quality, `test/integration-…` fails boundary, and the intersection is empty. Only `;` passes, and solely because `splitTaskList` (internal/core/md.go:91) does not split on `;` while the boundary gate does — a delimiter mismatch, not a designed escape.
- **Impact:** the tasks→executing approval is unreachable under `profile: production` for any spec whose design declares an external boundary. The message names no boundary, no artifact, and no remedy, so the only paths out are permuting the cell until a parser disagreement is found, or removing the boundary marker word from design.md to disarm `declaresExternalBoundary` — i.e. weakening the design to clear the gate. Two adjacent signals were silently ignored: `TaskRow.Kind` (internal/core/tasksparser.go:27) is never read by the policy, and the class name is never inspected, so `output_eval/integration-cli` refuses identically. The sibling finding never fired because its half uses `strings.Contains` on `Checks` (lenient substring) while the integration half uses `EqualFold` on a whole token (exact) — same function, two standards.
- **Recommendation:** have `BoundaryEvidenceFindings` parse the cell with `ParseQualityContract` rather than its own splitter, and satisfy the integration half when any `req.CheckID` contains `integration` or `strings.EqualFold(task.Kind, "integration")` — which accepts both artifacts already authored above. Add a regression test pinning one cell against both gates so a future divergence fails rather than deadlocks. Separately, make the finding name what it read: `evidence-policy: external boundary declared in design.<field> but no task declares integration evidence (set kind: integration, or an evidence check-id containing "integration")`. A gate that refuses without naming the artifact it inspected cannot be cleared by authoring.
- **Status:** open

### 2026-07-21 — friction — `allow_unknown_telemetry: false` is unsatisfiable, and it is what the shipped template tells production to set
- **Context:** managed project `aido`, spec `aido-config`, `profile: production`, `mode orchestrated` approved, `routing.allow_unknown_telemetry: false`. `specd brain run --authority`.
- **Expected:** the cost brake halts on a *measured* limit breach, per its own doc comment ("halts only on measured, reported telemetry").
- **Actual:** `halt (required telemetry unknown)`, exit 0, step 0, no mission minted. Not a cold-start condition — permanent:
  ```
  $ grep -rn "Observation:" internal/ | grep -v _test
  (no matches)
  ```
- **Root cause:** harness bug. `RequireTelemetry: !config.Routing.AllowUnknownTelemetry` (internal/cmd/brain_run.go:162) feeds `EvaluateBrakes`, which halts when `!snapshot.TelemetryKnown` (internal/orchestration/brakes.go:9). `TelemetryKnown` comes only from `AccrueTelemetry`, which folds `ACPEvent.Observation` off report events (internal/orchestration/telemetry.go:53) and returns a zero `Telemetry{}` when no report carries one. No production code path anywhere in the binary ever sets that field — only tests construct an `ObservationV1`. So the total is `Known=false` on every run of every spec, and the brake halts before the first dispatch, always. The loop has no entry point: telemetry requires a report, a report requires a claimed mission, a mission requires a dispatch, and the dispatch is what the brake refuses.
- **Impact:** `internal/core/embed_templates/project.yml:31` reads "Unknown telemetry is allowed by default for compatibility; **production policy should set it false**". An operator following the shipped guidance arms a permanent halt, and the halt exits 0 — so an unattended run reports success while dispatching nothing. Under `profile: production` there is no fallback route: `ProductionTaskAuthorityRequired` makes `verify` and `complete-task` refuse without an `AuthorityV1` packet (internal/cmd/dispatch.go:78), so the whole spec is frozen with no legal forward move by any actor.
- **Recommendation:** three, in order of value. (1) Do not ship guidance for a setting nothing can satisfy — either wire a real observation producer (host-measured duration at minimum, `source: "host"`, which `trustedSource` already accepts) or delete the "production should set it false" line and document the key as adapter-dependent. (2) Make the refusal name the missing producer rather than the missing value: `required telemetry unknown: no accepted report carries an observation; configure a telemetry-producing adapter or set routing.allow_unknown_telemetry: true`. (3) Validate the combination at config load — `allow_unknown_telemetry: false` with no telemetry-producing adapter configured is a `specd agents doctor` error, caught before a spec is authored rather than at step 0 of a run.
- **Status:** open

### 2026-07-21 — friction — `session ack` reports "mutable authority active" for a packet the CLI can never present
- **Context:** managed project `aido`, spec `aido-config`, `profile: production`. Granular route named by `specd drive`: `session open` → `context T1` (6 required lanes) → `session ack` → `session action` (nonce minted) → `specd verify aido-config T1`.
- **Expected:** the granular route completes. `specd drive` named it as the exact next command, `status --guide` lists `verify` as legal, and `session ack` printed `complete; mutable authority active`.
- **Actual:** `AUTHORITY_DENIED: production task command requires AuthorityV1 packet`. No flag on `verify` or `complete-task` accepts one.
- **Root cause:** harness bug — two distinct authorities share the name. `session ack` builds a real `AuthorityV1`, runs it through `AuthorizeWithReceipt`, then binds only `guarded.Digest` to the session (internal/cmd/session.go:189); the packet itself is discarded and never persisted. The packet that `runDispatch` requires arrives as a function parameter (internal/cmd/dispatch.go:42) whose only non-nil caller is `RunAuthorized`, called from exactly one place: `internal/cmd/mcp_exec.go:43`. Every CLI invocation passes nil, so under the production profile `verify` refuses unconditionally from the CLI regardless of session state. The MCP path does not rescue it either: `productionMissionScope` (dispatch.go:201) requires a lease whose `Authority.Digest` matches *and* a mission with matching role and `SubjectHead`, and session-issued authority mints neither. Only brain dispatch does.
- **Impact:** three surfaces — `drive`, `status --guide`, and `session ack` — jointly asserted a route that is structurally closed, and the agent spent a full five-command session reaching a refusal that was decidable at `session open`. Combined with the telemetry entry above, a production spec has no legal path forward by any actor: brain halts before dispatch, and the granular route cannot mint the packet dispatch alone issues.
- **Recommendation:** `drive` and `status --guide` must not name `verify`/`complete-task` as agent-legal when `ProductionTaskAuthorityRequired()` holds and no mission lease exists for the task — the correct next action is `brain step`/`brain run`, and the check is a lease lookup, not a judgement. Separately, `session ack` should say what it actually granted: `mutable authority active for session-scoped operations; task commands under profile: production require a mission-issued packet (specd brain run)`. A message that names a grant the next command rejects is worse than no message.
- **Status:** open

### 2026-07-21 — improvement — three structural deadlocks in one afternoon, all under a green CI: the production lane is proved by not being run
- **Context:** managed project `aido`, spec `aido-config`, the maximum-enforcement posture the shipped templates recommend — `profile: production`, `mode orchestrated`, `routing.allow_unknown_telemetry: false`. Three independent blockers in sequence (logged above): the evidence-policy cell with an empty satisfiable set, the permanently-unknown telemetry halt, and `verify` refusing a packet no CLI path can present. Full suite, `gofmt`, `go vet`, `docs-lint.sh`, `test-lint.sh`, and `regress-domains.sh` are all green against every one of them.
- **Observation:** none of the three is a deep bug. Each is a two-line disagreement between components that no test puts in the same room, and the pattern behind all three is the same: **the assertion and the production path are satisfied by different code.** Concretely, in the one script named for this posture:
  ```sh
  # scripts/production-smoke.sh:96
  # This installed-binary lane temporarily uses compatibility
  # profile for local task evidence/completion, then restores production before
  # production quality/review approval gates.
  cat >project.yml <<'EOF'
  profile: default
  EOF
  "$BIN" verify smoke T1 >/dev/null
  "$BIN" complete-task smoke T1 >/dev/null
  ```
  The production smoke test downgrades out of production to run the two verbs that refuse under production, then restores it. The refusal I spent a five-command session reaching is stepped around in CI, with a comment, and the lane still reports green. The same shape produced the telemetry halt: `ObservationV1` is constructed in tests and by nothing else in the binary, so `AccrueTelemetry` has full unit coverage over a population production never creates. And the same shape produced the authority mismatch: `RunAuthorized` is exercised through the MCP conformance proof, so the CLI's nil-authority path — the one every operator actually runs — is asserted nowhere.
- **Cost:** a full working day across three blockers, each requiring the harness's own source to diagnose because no error named the artifact it inspected. Two of the three are unreachable by any authoring: an operator following the shipped `project.yml` guidance ("production policy should set it false") arms a permanent halt that exits 0. The deeper cost is calibration — a green suite over a posture that cannot execute teaches an agent to distrust the suite, and the correct agent behaviour here (stop at two attempts, refuse to hand-write evidence) is indistinguishable from being stuck.
- **Recommendation:** four deterministic checks, cheapest first. None needs judgement; all are greps or table-driven tests.
  1. **Fixture-only field lint** (`scripts/test-lint.sh`): a persisted-schema field assigned in `_test.go` files and nowhere else is a dead contract. This catches `ACPEvent.Observation` exactly, and it is the check that would have prevented the whole telemetry class.
  2. **One parser per cell**: forbid `strings.Split`/`FieldsFunc` over a `TaskRow` field outside `internal/core/md.go`. Every gate reading the `evidence` cell must go through `splitTaskList`/`ParseQualityContract`. This makes the evidence-policy deadlock unwritable rather than merely fixed.
  3. **Guidance/dispatch parity test**: for each (phase × profile × orchestration mode), every `NextAction` that `status --guide` or `drive` labels `actor: agent` must survive `runDispatch`'s preconditions for that actor. A `verify` named as agent-legal while `ProductionTaskAuthorityRequired()` refuses it is then a failing test, not a five-command dead end. This is the highest-value one: it converts "three surfaces lied" into a red build.
  4. **No profile downgrades in the production lane**: delete the `profile: default` block from `production-smoke.sh` and let it fail. Whatever it takes to make that script pass end-to-end under an unbroken `profile: production` — including a real observation producer — is precisely the work these three entries are asking for. A smoke test permitted to leave the posture it is named for is measuring the other posture twice.
  Separately, and independent of the four: **`brain run` halting at step 0 having dispatched nothing exits 0.** An unattended run cannot distinguish "no work was ready" from "the controller cannot start". A distinct exit code for a halt with zero progress is one line and closes the silent-success hole.
- **Tradeoff:** checks 1, 2, and 4 are lints and a script edit — no runtime surface, no invariant touched, and 4 removes an exemption rather than adding one. Check 3 needs a table of profile/phase combinations, which is real test code to maintain, but it is a pure function of already-declared command metadata (`AllowedPhases`, `TaskRequired`, `Effect`) and no gate weakens. The exit-code change is behavioural and would need a note in the command reference. Nothing here adds a bypass; 4 removes one.
- **Status:** open

### 2026-07-21 — friction — `specd review <slug>` destroyed a 1011-line in-progress audit; the anti-clobber guard keys on the one field an auditor is known to leave stale
- **Context:** managed project `aido`, spec `aido-config`, phase `verify`, reading state as an operator. Command run: `specd review aido-config --json`, intending to *read* the recorded review verdict (`status --guide` had just named the review report in a blocker).
- **Expected:** a read. The verb name is a noun in the blocker text (`review.required: review report verdict ... is not one of approve|reject|needs-changes`), and every other spec-scoped verb I had run that turn (`status`, `check`) was read-only. `--json` is the read-shaped flag across the palette.
- **Actual:** `scaffolded .specd/specs/aido-config/review_report.md`, exit 0. A 1011-line audit — three passes, criterion-by-criterion trace, findings NN1–NN9 — replaced by the 59-line template. Recovered only because `.specd/` is committed in that project; `git checkout --` restored it. An operator who had not committed loses the entire report with no prompt and no backup.
- **Root cause:** harness bug, two independent ones.
  1. `internal/cmd/review.go:29` guards re-scaffold only when `core.ReviewReportHead(existing) == head`. The comment reasons "a stale report from an older commit is safe to replace." It is not. A stale `**Git HEAD:**` field is the *normal* state of a live multi-pass audit — this very report's own findings end with "the `**Git HEAD:**` field above still reads `5cffbab`" while HEAD was `d90efc4`. The guard is weakest exactly when the report is most valuable, and `--force` — documented as the way to overwrite — was never needed to destroy it.
  2. Unknown flags are accepted silently: `specd review aido-config --bogus-flag` also scaffolds and exits 0. `AGENTS.md` states unknown *verbs* fail closed (exit 2); flags do not, so a typo'd or wrong-shaped flag never surfaces the mistake.
- **Recommendation:**
  1. Invert the guard: refuse whenever the target exists, regardless of HEAD, unless `--force`. Message: `review report exists at <path> (HEAD field <x>, current HEAD <y>); pass --force to replace`. Staleness is information for the operator, not a licence for the harness. One-line change, and `--force` then means what its help text says.
  2. If a stale-HEAD re-scaffold must stay cheap, make `--force` write `review_report.md.bak` first — but the refusal is the correct default and needs no backup path.
  3. Fail closed on unrecognized flags for every verb (exit 2), matching the unknown-verb rule.
  4. Give the auditor a read path so no one reaches for `review` to inspect one: `specd status <slug> --json` should carry the parsed verdict, reviewer, and HEAD field of the review report. The blocker line already prints the verdict string; it is one struct field away from removing the reason I ran the write verb at all.
- **Status:** open

### 2026-07-21 — friction — the review gate rejects a verdict an auditor deliberately qualified, and the message quotes the whole 200-char line back
- **Context:** same spec/phase. `specd status aido-config --guide` blocker: `review.required: review report verdict "needs-changes — see \"third pass at 6d1fa4b\" at the end of this document; that subsection, not the original findings list, is the current call." is not one of approve|reject|needs-changes`.
- **Expected:** the auditor wrote `- **Verdict:** needs-changes — see "Third pass at 6d1fa4b" ...`, believing a qualified verdict reads as `needs-changes` plus a pointer. Multi-pass audits need to say *which* pass is current; the template gives them one line and no other field to say it in.
- **Actual:** exact-match parse over the whole remainder of the line, so the qualifier makes the verdict unparseable. Net effect: a report that says `needs-changes` in three places is treated as having no verdict, and the gate's blocker is indistinguishable from a missing report.
- **Root cause:** ambiguous docs plus a strict parser with no authoring affordance. Nothing in the scaffold comment says the field is exact-match to end-of-line.
- **Recommendation:** parse the first token of the verdict field and treat the rest as a free-text note (surface it in `status --json` as `review.note`). Failing that, make the template explicit — `- **Verdict:** <approve|reject|needs-changes>  <!-- exact, one word, nothing after -->` — and give multi-pass audits the field they actually need: `- **Pass:** <label>`. Also cap the quoted string in the blocker at ~60 chars; a 200-char echo buries the actionable half of the message.
- **Status:** open
### 2026-07-21 — friction — program view marks requirements-stage specs and dependencies complete
- **Context:** seven linked workflow improvement specs, phase perceive, agent author; exact command `./specd status --program`
- **Expected:** each spec reports active/incomplete at `requirements`, `workflow-01-truthful-control` is the planning frontier, and later specs report waiting on their predecessor.
- **Actual:** `workflow-01-truthful-control  phase=perceive (complete)`, every dependency renders `[complete]`, and `program frontier (actionable now):` is empty although every `state.json` status is `requirements`.
- **Root cause:** harness bug — program completion/dependency projection disagrees with authoritative per-spec lifecycle status.
- **Recommendation:** derive program completion and dependency satisfaction from loaded spec lifecycle state; add a black-box chain test with all specs at `requirements` that expects only the root spec actionable.
- **Status:** open

### 2026-07-22 — friction — `check --json` prints context-budget errors but exits 0
- **Context:** task planning for `workflow-03-state-foundations`, `workflow-05-executable-orchestration`, and `workflow-06-unattended-authority`; exact command `./specd check <slug> --json`
- **Expected:** a gate finding with `"severity":"error"` makes `check` exit 1, matching `docs/validation-gates.md` and allowing the driver loop to stop.
- **Actual:** output included `{"gate":"context-budget","severity":"error","message":"T16: required context 41141 tokens exceeds budget 40000 — decompose the task or narrow declared files"}` (and equivalent T25/T28 findings), but every command exited 0; the shell loop therefore reported `exit:0`.
- **Root cause:** harness bug — JSON rendering exposes failing findings but command exit semantics do not propagate their severity.
- **Recommendation:** make `runCheck` return the existing gate-failure error after rendering JSON when any finding is error severity; add CLI tests asserting text and JSON both exit 1 for a context-budget error.
- **Status:** open

### 2026-07-22 — improvement — generated Codex MCP config can select a different binary than the bootstrap pin
- **Context:** configuring Codex Brain hosting in this repository; exact commands `./specd init --agent=pinky`, `./specd handshake bootstrap workflow-01-truthful-control --json`, and `codex mcp list`.
- **Observation:** bootstrap pinned local `./specd` at commit `062c54632013d41f0be3e26409ab4e35efbf1962`, while generated `.codex/config.toml` contained `command = "specd"`; `command -v specd` resolved `/usr/local/bin/specd` at commit `2549cf56dbc26de4bbea6fea8cb402c9468f68f8`.
- **Cost:** the Codex MCP host would execute a different schema/palette/config implementation than the one whose handshake the worker received. Detecting it required comparing both `version --json` records, then manually pinning `command = "./specd"` and re-running `codex mcp list`.
- **Recommendation:** have `writePinkyArtifacts` register the same resolved binary used by the initiating process, or default repository scaffolds to `./specd` when that executable exists; make `agents doctor` compare the MCP command's version/commit with the active handshake pin and report an actionable mismatch.
- **Status:** open

### 2026-07-22 — friction — task context does not distinguish a declared new file from a missing required source
- **Context:** Brain/Pinky execution of `workflow-01-truthful-control` T01. The task declares new file `docs/workflow-regressions.md`; exact read command included `sed -n '1,320p' docs/workflow-regressions.md`.
- **Expected:** the bounded task context identifies declared paths that do not exist yet, so a worker can skip source loading and create them during implementation.
- **Actual:** the read emitted `sed: can't read docs/workflow-regressions.md: No such file or directory`. The manifest omitted the path from eager source items but did not explicitly classify it as a new-file target.
- **Root cause:** context manifest models existing declared files as source items and silently omits absent declared files; the worker must derive that omission by comparing the task row with manifest items.
- **Recommendation:** add a deterministic `declared_new_files` array to `specd context --json`, populated from declared task files absent at manifest construction. This removes a failed read without widening scope.
- **Status:** open

### 2026-07-22 — improvement — verifying a completed task advertises an already-consumed completion
- **Context:** after `specd brain report workflow-01-truthful-control 0f24c7649ad389eb41b874b721678b88 pinky-t01` completed T01, the host re-pinned evidence to the checkpoint commit with exact command `./specd verify workflow-01-truthful-control T01`.
- **Observation:** verify passed but printed `evidence recorded for workflow-01-truthful-control T01; task not complete; run \`specd complete-task workflow-01-truthful-control T01\`` although T01 already carried the completed marker.
- **Cost:** the host must re-read task state before trusting the exact next command; following it would create a needless refusal/round trip.
- **Recommendation:** after recording evidence, inspect the parsed task marker. For an already completed task, print `evidence refreshed for <slug> <task>; task already complete` and omit `complete-task` guidance.
- **Tradeoff:** one existing parsed-state branch; no gate or evidence semantics change.
- **Status:** open

### 2026-07-22 — friction — serial Brain missions cannot cross the previous task marker checkpoint
- **Context:** `workflow-01-truthful-control` T02 under Brain/Pinky. T01 had completed through `brain report`, leaving the harness-written T01 marker dirty in `.specd/specs/workflow-01-truthful-control/tasks.md`. Exact first command: `./specd brain report workflow-01-truthful-control 386174e11d4cc23f16a4cff5a74066da pinky-t02`.
- **Expected:** a serial Brain session can report T02 after Brain itself completed T01; controller-owned T01 state cannot become an outside-scope T02 mutation.
- **Actual:** first report failed with `OUTSIDE_SCOPE: task T02 changed files outside its declared scope:` followed by `T02 modifies harness-owned state .specd/specs/workflow-01-truthful-control/tasks.md; task markers, roles, and steering are written by specd verbs, never edited directly`. The host committed only that harness-written marker, re-ran `./specd verify workflow-01-truthful-control T02` successfully, and retried once. Retry failed with `REPORT_BASELINE_STALE: report pins afd62f968e62e46b2f3aa9a74fd45f28711a0004 but mission workflow-01-truthful-control.s2.T02 pinned c2efdf017ca5e08658379f1fe49f7412a9a976ea` and named no recovery command.
- **Root cause:** the next mission inherits a Git baseline that excludes the previous `brain report` task-marker mutation, but task completion later classifies that controller-owned dirty marker as a worker edit. Checkpointing it advances HEAD and invalidates the already-issued mission, producing a two-error dead end.
- **Recommendation:** after a successful Brain report, persist a controller-owned post-completion baseline for subsequent mission scope checks, excluding the exact task/state mutation Brain just wrote. If HEAD changes after dispatch, `REPORT_BASELINE_STALE` must name and support one deterministic reissue command that revokes the stale lease and re-mints the same task at current HEAD.
- **Status:** open

### 2026-07-22 — friction — shared readiness initially bypassed typed slug validation
- **Context:** `workflow-01-truthful-control` T04, after routing check and approval through a shared readiness builder. Exact command: `go test ./internal/core ./internal/cmd -count=1`.
- **Expected:** `check` rejects traversal slugs through the existing typed invalid-slug boundary before resolving any state path.
- **Actual:** the first run failed with `--- FAIL: TestTypedRefusalDispatchSitesAreTyped/traversal-slug` and `refusal_test.go:54: untyped refusal: open /tmp/TestTypedRefusalDispatchSitesAreTyped981945509/001/escape/state.json: no such file or directory`; it also failed with `--- FAIL: TestSlugTraversalRejectedForSlugReason/check` and `traversal_test.go:115: check [../../specd_traversal_canary]: want invalid-slug rejection, got open /tmp/TestSlugTraversalRejectedForSlugReasoncheck3864853779/001/specd_traversal_canary/state.json: no such file or directory`.
- **Root cause:** the new readiness builder loaded state before `loadSpec` could perform the validation the previous check path inherited indirectly.
- **Recommendation:** keep slug validation explicit at the shared readiness boundary and retain both typed-refusal and traversal regression cases whenever readiness input assembly changes.
- **Status:** resolved — `go test ./internal/cmd -run 'TestTypedRefusalDispatchSitesAreTyped|TestSlugTraversalRejectedForSlugReason' -count=1`.

### 2026-07-22 — friction — focused route parity omitted legacy machine-surface compatibility
- **Context:** `workflow-01-truthful-control` T05 after adding route-aware guidance and MCP preflight. Exact command: `go test ./internal/core ./internal/cmd ./internal/mcp -count=1`.
- **Expected:** new route projection keeps production authority packets executable and preserves existing drive/locator machine fields while separating handoffs.
- **Actual:** the first broader run failed with `dispatch_test.go:97: MCP executor dropped authority packet: ROUTE_HANDOFF_REQUIRED: operation complete-task requires a operator handoff`, `drive_test.go:274: drive lists 2 legal operations, guide lists 6`, and `locator_test.go:75: "task" listed as both legal and human-only` for both `status_--json` and `status_--guide_--json`.
- **Root cause:** the focused T05 parity matrix covered new classifications but did not initially include existing consumers that define compatibility semantics for supplied authority, granular guide counts, and mutually exclusive locator lists.
- **Recommendation:** make the documented pre-verify command run all three touched packages, or include these compatibility cases under the T05 verify regex, so new route projections cannot pass focused parity while breaking legacy machine fields.
- **Status:** resolved — `go test ./internal/core ./internal/cmd ./internal/mcp -count=1`.

### 2026-07-22 — friction — feedback excerpt lookup built an invalid sed range after no match
- **Context:** `workflow-01-truthful-control` T06 diagnosis. Exact command: `rg -n "EVIDENCE_MISSING says" AIDO-WORKFLOW-FEEDBACK.md && line=$(rg -n "EVIDENCE_MISSING says" AIDO-WORKFLOW-FEEDBACK.md | cut -d: -f1 | head -1); start=$((line-8)); end=$((line+36)); sed -n "${start},${end}p" AIDO-WORKFLOW-FEEDBACK.md`.
- **Expected:** either print the matching feedback excerpt or stop cleanly when no heading matches.
- **Actual:** `sed: invalid option -- '8'`; the empty line number became a negative range. No repository write occurred.
- **Root cause:** the exploratory compound command did not validate the `rg` result before arithmetic.
- **Recommendation:** use `rg -n` alone first, or guard `[ -n "$line" ]` before computing a sed range.
- **Status:** resolved — operator command discipline; no product change.

### 2026-07-22 — friction — MCP refusal mapping assumed a task field absent from adapter requests
- **Context:** T06 focused preflight. Exact command: `gofmt -w internal/core/refusal.go internal/core/refusal_test.go internal/cmd/refusal_test.go internal/cmd/check.go internal/cmd/lifecycle.go internal/cmd/verify.go internal/cmd/brain_run.go internal/mcp/mapping.go && go test ./internal/core ./internal/cmd ./internal/mcp -run TestRefusalRecoveryContract -count=2`.
- **Expected:** typed MCP refusal mapping compiles against the existing adapter request contract.
- **Actual:** `internal/mcp/mapping.go:18:20: req.TaskID undefined (type adapter.Request has no field or method TaskID)`.
- **Root cause:** refusal entity projection inferred a task-specific field instead of using the request's existing operation identity.
- **Recommendation:** keep mapping limited to fields declared by `adapter.Request` and retain the cross-package refusal contract compile check.
- **Status:** resolved — `go test ./internal/core ./internal/cmd ./internal/mcp -run TestRefusalRecoveryContract -count=2`.

### 2026-07-22 — friction — typed evidence refusal initially broke a legacy dispatch detail contract
- **Context:** T06 broader compatibility run. Exact command: `gofmt -w internal/core/refusal.go internal/core/refusal_test.go internal/cmd/refusal_test.go internal/cmd/check.go internal/cmd/lifecycle.go internal/cmd/verify.go internal/cmd/brain_run.go internal/mcp/mapping.go && go test ./internal/core ./internal/cmd ./internal/mcp -count=1`.
- **Expected:** the additive typed refusal fields preserve legacy diagnostic substrings consumed by existing boundary tests.
- **Actual:** `dispatch_test.go:94: authorized route did not reach evidence gate: EVIDENCE_MISSING: task T1: no task verify record`.
- **Root cause:** the new truthful `observed` classification replaced the old `requires passing evidence` detail instead of preserving it as compatibility text.
- **Recommendation:** keep stable legacy detail in `detail` while structured fields carry the normative missing/failing/stale classification; retain the broader affected-package run.
- **Status:** resolved — `go test ./internal/core ./internal/cmd ./internal/mcp -count=1`.

### 2026-07-22 — friction — production smoke source assertions searched for backslashes absent from the script
- **Context:** Release A exit verification after T06. Exact commands: `go test ./... -race -count=1` and `go test ./... -count=2`.
- **Expected:** both full suites accept the production smoke script's literal JSON request and refusal assertion.
- **Actual:** both failed `TestProductionSmokeLane` with `production_smoke_test.go:34: production smoke omits "\\\"method\\\":\\\"initialize\\\""`, `production_smoke_test.go:34: production smoke omits "\\\"driver_capabilities\\\":{}"`, and `production_smoke_test.go:34: production smoke omits "\\\"capability\\\":\\\"sandbox\\\",\\\"status\\\":\\\"refused\\\""`.
- **Root cause:** raw-string test contracts retained JSON escape backslashes even though `scripts/production-smoke.sh` stores shell-quoted JSON without those backslashes.
- **Recommendation:** assert the exact literal script text and keep `TestProductionSmokeLane` in the full release suite.
- **Status:** resolved — `go test ./internal/integration -run TestProductionSmokeLane -count=1`.

### 2026-07-22 — friction — shared readiness disarmed malformed quality declarations during early phases
- **Context:** Release A domain regression verification. Exact command: `./scripts/regress-domains.sh`.
- **Expected:** AD-R1 rejects a malformed task evidence declaration during `specd check` before execution.
- **Actual:** `[AD-R1] VIOLATION: check accepted malformed evidence declaration`.
- **Root cause:** shared readiness supplied the current lifecycle gate instead of the former empty check target, while `qualityDeclarationArmed` excluded requirements and design; the structural check silently disappeared in those phases.
- **Recommendation:** arm malformed quality-declaration validation for every lifecycle readiness phase and retain AD-R1 as the black-box invariant.
- **Status:** resolved — `./scripts/regress-domains.sh`.

### 2026-07-22 — friction — final readiness reapplied context budgeting to completed T05
- **Context:** final Release A readiness after every task completed through Brain. Exact command: `./specd status workflow-01-truthful-control --guide`.
- **Expected:** execution readiness evaluates remaining work; completed tasks cannot require another context manifest.
- **Actual:** `blocker: T05: required context 41256 tokens exceeds budget 40000 — decompose the task or narrow declared files`.
- **Root cause:** `contextBudget` iterated every task without consulting the marker-derived completion status already present in `CheckCtx`.
- **Recommendation:** skip completed tasks in the shared context-budget gate and pin the terminal-work case directly.
- **Status:** resolved — `go test ./internal/core/gates -run TestContextBudget -count=2`.

### 2026-07-22 — friction — first unfinished spec is not directly executable
- **Context:** starting the requested Brain/Pinky run for `workflow-03-state-foundations`. Exact command: `specd status workflow-03-state-foundations --guide --json`.
- **Expected:** status identifies the next legal task or a single human handoff needed to enter execution.
- **Actual:** status remained at plan/tasks and reported `T16A: required context 40777 tokens exceeds budget 40000 — decompose the task or narrow declared files`, while `mode` and `approve` were only listed separately as human-only operations.
- **Cost:** the host must inspect task decomposition before it can tell the human whether mode activation, artifact repair, or approval comes first.
- **Recommendation:** when a pre-execution gate blocks, `--guide` should emit one ordered recovery sequence, including the artifact/role authorized to fix the blocker and the subsequent human-only commands.
- **Tradeoff:** guidance-only projection; no authority or gate semantics change.
- **Status:** open

### 2026-07-22 — friction — context-budget refusal omits a decomposition route
- **Context:** Pinky scout inspection of the blocked plan. Exact command: `./specd context workflow-03-state-foundations T16A --json`.
- **Expected:** refusal names the oversized contributors or a legal command/role for narrowing the task.
- **Actual:** command failed with `required context 40777 tokens exceeds budget 40000`; resolving it required manually measuring the nine declared files and deriving a two-task split.
- **Cost:** one extra repository-analysis round trip before the operator can approve the plan.
- **Recommendation:** include per-source token contributions and an authorized next action in context-budget refusals.
- **Tradeoff:** diagnostic output only; the deterministic budget and authority remain unchanged.
- **Status:** open
