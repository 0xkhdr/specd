# Project agent guide

This project uses `specd`, an agent-agnostic, spec-driven harness. The full
harness contract, workflow, and steering pointers live in `AGENTS.md`, imported
below so it stays the single source of truth — do not duplicate prompt content
here.

@AGENTS.md
