# Requirements - demo

## R1

When the demo spec is inspected, it shall expose a small deterministic task graph.

## R2

When task frontier is requested, it shall be derived only from `tasks.md` and `state.json`.
